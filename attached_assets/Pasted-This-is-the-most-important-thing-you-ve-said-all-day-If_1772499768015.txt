This is the most important thing you've said all day. If sandbox data leaked into Statewide's workspace, that's a production contamination issue that has to be resolved before you go live. Real invoices mixed with fake data, real employees mixed with test accounts, real clients mixed with seed clients — that makes everything untrustworthy.Prepared to clarify environment separation and data integrity concernsPrepared to clarify environment separation and data integrity concernsCreating a workspace isolation and production readiness prompt for Replitworkspace-isolation-spec.mdGood, created the file. Let me present it to Bryan.Presented fileGood, presented to Bryan. Let me give him the summary.DoneThis is the spec that protects your real business. Here's the core of it:
Three workspace types, completely isolated:
Sandbox (dev-acme-security-ws, etc.) — test data playground. Seed scripts, training tools, resets all allowed. This is where Trinity experiments and where the agent runs audits.
Production (Statewide Protective Services) — your real company. Zero test data. Zero seed scripts. Zero training tools touching it. Every record is real or entered by you. When you open Statewide, it should be factory-fresh — zero clients, zero employees, zero shifts. You add your real clients. You add your real guards. Trinity learns YOUR business from scratch.
Platform (CoAI support org) — persistent, never reset, never seeded. You, Trinity, and HelpAI live here handling support operations. This org survives everything.
The safeguard is simple: a workspace_type field on every workspace, and a guardAgainstProduction() check at the top of every seed, reset, and training function. If the workspace is production or platform, the function throws an error and refuses to run. Period. Only sandbox workspaces allow destructive operations.
The first thing the agent needs to do is audit Statewide RIGHT NOW — query every table in that workspace and show you exactly what's in there. If there's a single fake client, test employee, or seeded shift, it gets removed. Statewide needs to be spotless before you put your first real guard on a real shift.
Paste this alongside the other safeguard work it's already doing. This is the wall between your test environment and your livelihood.