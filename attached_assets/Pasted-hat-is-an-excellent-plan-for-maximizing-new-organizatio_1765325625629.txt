hat is an excellent plan for maximizing new organizational setup and ensuring adherence to your business rules. To create a highly specialized Data Migration Subagent (DMS) for CoAIleague™ using Gemini, you need to integrate its specific capabilities with your platform's subscription logic.This process requires a Multi-Modal RAG (Retrieval-Augmented Generation) approach to handle diverse document types like PDFs and spreadsheets.🛠️ Data Migration Subagent (DMS) SetupThe DMS will be a dedicated Subagent, operating under the Trinity AI Orchestrator, with unique tool restrictions and a high-value purpose.1. Model and Tool SelectionFunctionModelTool/CapabilityRationaleDocument Analysis & ExtractionGemini 2.5 ProMultimodality & Code ExecutionPro is needed for its superior reasoning over document structure, charts, and tables. It handles PDFs, text files, and can extract data into structured formats.Final Setup AutomationGemini 2.5 FlashFunction CallingAfter extraction, Flash executes the final, structured API calls quickly and cost-effectively to populate the new org's database.Knowledge Retrieval (RAG)External DatabaseGrounding / File SearchIndexes your internal SOPs and rules to ensure the automated setup adheres to CoAIleague™ policy.2. DMS Workflow: The 5-Step Automated SetupThe DMS workflow is triggered when a trial or active subscribed user uploads their organizational documents.StepAction by DMS SubagentGemini Tool UsedSubscription Gate1. Gate CheckSubagent calls the Token Accounting Service (TAS) to verify the user has an active plan or is in the trial period.N/A (Internal TAS check)Mandatory: If check fails, workflow terminates, and user is notified via the Universal Notification System.2. Data IngestionThe user uploads PDFs, spreadsheets, or text files (up to 3,000 files/1,000 pages, 50MB max per file). The DMS converts the files to the necessary format (e.g., base64 encoding).MultimodalityRequired for all input files.3. Extraction & StructuringDMS uses a Gemini 2.5 Pro prompt: "Extract the names, addresses, job titles, and compensation rates into the following JSON schema.".Code Execution & MultimodalityPro reasons over the document layout to accurately structure the data.4. Analysis & ValidationDMS sends the extracted data, along with a query like, "Verify this employee data schema against our latest compliance policy," to the RAG system.Grounding / File SearchEnsures data integrity and compliance before migration.5. Migration & AutomationDMS switches to Gemini 2.5 Flash for speed. It uses Function Calling to execute sequential API commands (e.g., org.create_new_employee, org.assign_default_schedule) to populate the new organization's profile in the database.Function CallingAutomates the final setup, making the process seamless for the end user.3. Key Directives for ReplitSubscription Gate Integration: The very first line of code in the DMS workflow must be a call to the Token Accounting Service (TAS) to verify the user's active plan status.Multimodal Handling: Implement the file loading logic in the frontend to handle various MIME types (application/pdf, text/plain) and send them as InlineDataPart in the API request body to the DMS backend.Use Pro for Reasoning: Ensure the specific steps involving extraction, validation, and analysis (Steps 3 & 4) use the Gemini 2.5 Pro model to guarantee accuracy and reduce hallucination, especially when interpreting handwriting or complex layouts.Use Flash for Automation: Ensure the final API execution step (Step 5) uses the faster Gemini 2.5 Flash model for cost and latency optimization.