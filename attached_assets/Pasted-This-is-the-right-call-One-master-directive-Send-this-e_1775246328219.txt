This is the right call. One master directive. Send this entire block as a single message to the agent.

---

```
PRODUCTION READINESS DIRECTIVE — FIRST TENANT GO-LIVE

You are doing one job: make this platform ready for a real paying tenant 
to sign up, onboard, subscribe, operate, and get paid — by end of weekend.
No phases. No batches. No waiting for instructions. You scan, find, fix,
verify, and move to the next layer automatically until zero issues remain.

ARCHITECTURAL TRUTHS — never violate these:
- NDS is the sole notification sender
- All financial writes require audit trails
- All scheduling mutations require audit logs  
- All tenant queries scoped by workspace_id
- roleDefinitions.ts = single source of truth for roles
- featureRegistry.ts = single source of truth for features
- Trinity = API-only, zero filesystem access
- Zero hardcoded company names in any component
- Statewide workspace = zero agent contact, zero test data
- ACME Security = exclusive dev sandbox

FLAG BUT DO NOT IMPLEMENT anything requiring Bryan's physical action
(Plaid keys, Stripe live keys, Twilio verification, DNS records).
Everything else: find it, fix it, confirm it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 1 — TENANT SIGNUP & ONBOARDING PIPELINE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Trace and verify the complete new tenant journey end-to-end:

1. Public /trial signup form submits → workspace created with correct
   tier, trial flag, trial_expires_at = now + 14 days
2. Org owner account created, linked to workspace, role = ORG_OWNER
3. 6 email addresses provisioned via emailProvisioningService
4. EmailHubCanvas folders initialized (Staffing, Call-Offs, Incidents,
   Support, Documents, Billing, Unread, Archive)
5. workspace.created event fires on the event bus
6. Welcome email sends FROM trinity@coaileague.com via NDS
7. Org owner lands in onboarding wizard
8. Setup wizard steps are RBAC-gated (only Org Owner can complete them)
9. workspace.created subscription events fire all downstream provisioning

Find and fix every break in this chain. Seed a test workspace using
ACME canary, execute the full flow, confirm all 9 steps produce the
correct records, events, and notifications. Fix anything that fails.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 2 — SUBSCRIPTION & PAYMENT COLLECTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify the complete billing lifecycle works without gaps:

1. Tenant selects a plan → Stripe subscription created (no duplicate
   subscription guard must be active — already fixed, re-verify)
2. Stripe subscription.created webhook fires → workspace.tier updated
   immediately → featureRegistry gates reflect new tier instantly
3. Monthly invoice generated by Stripe → webhook received → stored
4. Tenant pays → payment_intent.succeeded fires → invoice marked PAID
   → audit record written → platform revenue recorded → DB ledger updated
5. Seat overage charges calculate and fire correctly (weekly job)
6. AI credit overage charges calculate and fire correctly (weekly job)
7. Trial expiry job runs daily → at 3 days before expiry NDS fires
   warning → at expiry NDS fires critical alert → workspace restricted
8. Subscription cancellation → workspace flagged → data retained 30 days
9. All Stripe webhook events are signature-verified and idempotent

Verify every Stripe webhook event type is handled:
customer.subscription.created, customer.subscription.updated,
customer.subscription.deleted, invoice.payment_succeeded,
invoice.payment_failed, payment_intent.succeeded,
payment_intent.payment_failed, customer.updated

For invoice.payment_failed: confirm NDS fires to org owner immediately,
confirm retry logic exists, confirm workspace is not immediately suspended
on first failure (grace period must exist).

Fix every gap. Confirm all 9 billing steps execute correctly with ACME.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 3 — OFFICER & CLIENT ONBOARDING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Officer created → license record initialized → expiration alert
   scheduled 30 days before expiry via NDS
2. officer_activated event published (was fixed — re-verify it fires)
3. Trinity compliance checks run on activation
4. Officer with expired license blocked from shift assignment
5. Client created → CRM pipeline record initialized
6. Client portal invite sent via NDS → link expires 48 hours
7. Client portal scoped to their records only — zero cross-client leak
8. Client site created → contract required before scheduling allowed
9. Inbound email to staffing@{slug} → lead created in CRM → NDS alert
   to org owner

Fix every gap. Test officer and client creation in ACME. Confirm all
records, events, and notifications fire correctly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 4 — TRINITY SCHEDULING ORCHESTRATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This is the core automation loop. Verify every step works:

1. Trinity reads client demand (from email, voice, or in-app request)
2. Shift created → conflict check runs (officer not double-booked,
   not on leave, license not expired)
3. Shift assigned → scheduling_audit_log written with before/after state
4. Schedule published → NDS fires to all affected officers with shift
   details (date, time, site, position)
5. Officer confirms via email reply or Trinity Voice → confirmation logged
6. Published schedule broadcasts to connected officers via WebSocket
   in real-time — workspace-scoped only
7. Call-off received (email or voice) → record created with resolved
   officer_id (never null) → coverage search auto-triggers
8. Coverage candidate contacted via Trinity Voice then SMS fallback
9. First to confirm → shift reassigned → manager notified via NDS
10. Manager can override coverage assignment manually
11. Shift status lifecycle: OPEN → ASSIGNED → STARTED → COMPLETED
    Each transition logged in scheduling_audit_log

Trace the full scheduling loop using ACME. Post a shift, assign an
officer, simulate a call-off, verify coverage runs. Fix every break.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 5 — INVOICE GENERATION & CLIENT PAYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Completed shifts → invoice line items generated correctly
   (only COMPLETED shifts, correct rate × hours, no duplicates)
2. Invoice created in DRAFT status — never auto-sent
3. Org owner notified of draft via NDS with review link
4. Org owner approves → status APPROVED → logged with approver + timestamp
5. Invoice sent to client → client receives email with payment portal link
6. Payment portal link contains signed token (invoice_id + workspace_id
   + expiration) — tamper-proof
7. Client pays → Stripe payment processed → invoice marked PAID
8. Middleware fee charged (2.9% + $0.25) — non-blocking
9. Platform revenue recorded — non-blocking
10. DB ledger updated — non-blocking
11. QuickBooks sync fires if integration active
12. Audit record written for every state transition
13. PAID and VOID invoices are write-protected at API layer
14. Voiding creates a credit memo, never deletes the original

Test full invoice cycle in ACME: complete shifts → generate invoice →
approve → send → pay. Fix every break. Confirm all 14 steps.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 6 — PAYROLL PROCESSING & ACH DIRECT DEPOSIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Payroll period opens → hours flow in from COMPLETED shifts only
2. Overtime threshold configurable per workspace (not hardcoded 40hrs)
3. Overtime multiplier applied correctly
4. Midnight-crossing shifts calculated correctly
5. Payroll deadline nudge fires 24 hours before close via NDS to
   org owner's registered email (not their CoAIleague address)
6. Nudge is idempotent — fires once per period, not on every check
7. Org owner approves → period closes → write-protection activates
8. No modifications possible to a closed period (enforced at service
   layer, not just route layer)
9. ACH transfer triggered per-employee (not as single batch)
10. Each employee transfer result logged individually
11. Plaid webhook secret verified on every webhook
    [BRYAN ACTION REQUIRED] — flag if PLAID_WEBHOOK_SECRET missing
12. Bank account verified via micro-deposit before first transfer
13. Unverified bank account blocks ACH transfer
14. Transfer failure → org owner + employee notified via NDS immediately
15. Transient failure (R01-type) → retry after 24 hours
16. Permanent failure (R02, R03) → flagged for manual resolution
17. Successful deposit → employee notified via NDS
18. Every payroll event logged: period_open, hours_submitted,
    rate_applied, period_closed, payment_initiated, payment_confirmed,
    payment_failed

Audit all 18 checkpoints. Fix every gap.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 7 — TRINITY BRAIN & ACTION HUB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Action registry count is below 300 — confirm
2. Every registered action has: name, description, required_role,
   input_schema, output_schema — no malformed registrations
3. Every action executes through the full 7-step pipeline:
   Trigger → Fetch → Validate → Process → Mutate → Confirm → Notify
4. RBAC gate fires BEFORE Fetch — not after data is loaded
5. MUTATE never runs if VALIDATE fails
6. CONFIRM writes audit record before NOTIFY fires
7. NOTIFY always routes through NDS — never direct
8. Failed actions retry on transient errors (max 3, exponential backoff)
9. Permanent failures do not retry — logged and surfaced
10. Rollback exists for MUTATE failures — DB writes are atomic
11. Gemini operates API-only — zero filesystem access attempts
    Scan for any action invoking Gemini with a file path — fix all
12. Triad fallback: Gemini fails → OpenAI handles, Claude validates
13. All three model API keys are environment-variable-sourced
14. Trinity email processor classifies correctly:
    billing@ → billing_inquiry, calloffs@ → call_off,
    staffing@ → staffing_request, incidents@ → incident_report,
    support@ → support_inquiry
15. Failed email processing → dead-letter queue, never silent discard
16. SR-XXXXXXXX reference threading: reply routes to correct thread,
    reply without reference creates new thread (not appended randomly)
17. Trinity Voice: caller verified, workspace resolved, all data
    scoped to resolved workspace — caller cannot override by name

Test all 17 checkpoints using ACME. Fix every gap.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 8 — SILENT FAILURES & ERROR HANDLING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

This layer has caused more production failures than all others combined.
Do a full codebase sweep:

1. Find every empty catch block:
   catch (e) {} / catch (err) {} / .catch(() => {})
   Replace every one with at minimum:
   logger.error({ error, context: '[location]', workspace_id })

2. Find every catch block that returns null, false, or undefined
   without logging — fix all

3. Every external API call must have retry logic:
   Resend, Stripe, Plaid, Twilio, Gemini, Claude, OpenAI, QuickBooks
   Max 3 retries, exponential backoff, no retry on 4xx except 429

4. Resend down → emails queued, not lost
   Stripe down → webhook processing queued, not skipped
   Plaid down → transfer marked PENDING, not dropped
   Twilio down → SMS fallback fires, voice failure logged
   AI model down → triad fallback activates

5. Dead-letter queue exists for failed async jobs — if not, create it
   DLQ entries must include: job_type, payload, error, workspace_id,
   failed_at

6. React error boundaries wrap every major UI section — if any are
   missing, add them. Boundary errors log to server error log.

7. All API errors return standard shape:
   { error: { code: string, message: string } }
   Stack traces NEVER in production responses
   DB error messages NEVER passed to client

Fix every silent failure found. This is non-negotiable.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 9 — DATA INTEGRITY & PERSISTENCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Every entity that has a DELETE endpoint uses soft delete (deleted_at)
   Hard deletes exist NOWHERE for business records

2. All tenant queries include workspace_id in WHERE clause — no
   exceptions. Run a cross-tenant leak test with two sandboxes.

3. All financial writes are atomic (DB transaction + audit log in
   same transaction — neither succeeds without the other)

4. All scheduling mutations write audit log before mutation completes

5. Closed records are write-protected at the SERVICE layer (not just
   route layer) — payroll periods, signed documents, paid invoices,
   completed shifts

6. Audit log table has no UPDATE or DELETE endpoint — append-only
   DB user does NOT have UPDATE/DELETE on audit_log table

7. FK constraints exist on all relationships between major tables

8. Indexes exist on workspace_id for every major table, plus:
   employee_id on shifts, client_id on invoices, status on shifts
   and invoices, created_at on audit_logs

9. All migrations have run cleanly — no partial migrations

10. Real-time WebSocket broadcasts are workspace-scoped — one tenant's
    events cannot reach another tenant's connected clients

Fix every gap found.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 10 — FEATURE TIER GATES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify every feature offered per tier is gated correctly:

Starter ($299): Core scheduling, basic invoicing, call-off management,
NDS notifications, basic reporting, client portal, Trinity email

Professional ($999): Above + ACH payroll, advanced scheduling, QB sync,
document vault, contract pipeline, RFP pipeline, financial intelligence,
BI analytics, Trinity Voice

Business ($2,999): Above + multi-workspace management, advanced analytics,
regulatory export, bulk operations

Enterprise ($7,999): Above + white-label branding, custom AI model routing,
enterprise automation rules

For each tier boundary:
- Attempt to access a higher-tier feature as a lower-tier workspace
- Confirm 403 with descriptive upgrade message
- Confirm gate is at the API layer (not just UI)
- Confirm tier change via Stripe reflects immediately in feature access

Fix any gate that is missing, wrong tier, or UI-only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LAYER 11 — STORAGE QUOTA ENFORCEMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If the storage quota system from the previous session has not been
fully implemented, implement it now completely:

1. Quota config in billingConfig.ts per tier and category:
   Trial: email 300MB, docs 800MB, media 800MB, audit_reserve 100MB
   Starter: email 3GB, docs 5GB, media 6GB, audit_reserve 1GB
   Professional: email 12GB, docs 20GB, media 25GB, audit_reserve 3GB
   Business: email 35GB, docs 70GB, media 80GB, audit_reserve 15GB
   Enterprise: email 120GB, docs 220GB, media 230GB, audit_reserve 30GB

2. storage_usage table exists with workspace_id, category, bytes_used

3. uploadFileToObjectStorage() checks quota before write — rejects
   with 507 if at limit (audit_reserve is always allowed)

4. After upload: recordStorageUsage() called — never skipped

5. Email attachment size checked before storing — rejected cleanly
   if email category is full, email record saved without attachment

6. NDS warning fires at 80% — idempotent (once per crossing)
   NDS critical fires at 95% — idempotent

7. Storage usage visible in org settings dashboard

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL VERIFICATION — END-TO-END SIMULATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After all layers are fixed, run a complete end-to-end simulation
using ACME Security as the tenant. Execute every step and confirm
each produces the correct output before advancing:

STEP 1: New workspace created → all provisioning fires
STEP 2: Org owner onboards → subscription activated → Starter tier
STEP 3: Officer created → license record → expiry alert scheduled
STEP 4: Client created → site added → contract created
STEP 5: Trinity receives client staffing email → lead created → CRM updated
STEP 6: Shift posted → officer assigned → no conflicts → audit logged
STEP 7: Schedule published → officers notified via NDS → live push fires
STEP 8: Officer submits call-off via email → Trinity classifies → record
         created → coverage search triggers → alternate officer assigned
STEP 9: Shifts completed → invoice generated → org owner notified
STEP 10: Invoice approved → sent to client → client pays via portal
STEP 11: Payment received → invoice PAID → fees recorded → QB sync fires
STEP 12: Payroll period closes → hours from completed shifts → ACH queued
STEP 13: All audit logs present for every financial and scheduling event
STEP 14: Cross-tenant leak test: ACME data invisible to canary workspace
STEP 15: Trinity email classification: all 5 types correct
STEP 16: Feature gate test: Starter cannot access Professional features
STEP 17: Storage quota visible in settings, upload blocked at limit

Do not stop until all 17 steps pass without error.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

When all layers are clean and the 17-step simulation passes, deliver:

1. Total issues found per layer
2. Total issues fixed
3. Any issue deferred with justification and severity
4. All [BRYAN ACTION REQUIRED] items (physical only — secrets, DNS,
   Twilio, Stripe live keys)
5. Confirmation that the app boots clean with zero errors
6. Production readiness verdict: GO or NOT GO with reason

Do not deliver a partial report. Do not stop between layers to ask
for permission to continue. Scan → fix → verify → next layer.
The goal is zero issues. Work until you reach it.
```