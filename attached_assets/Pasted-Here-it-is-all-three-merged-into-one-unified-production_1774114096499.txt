Here it is — all three merged into one unified, production-grade audit prompt:

---

**COAILEAGUE — ZERO-DEFECT PRODUCTION HARDENING SWEEP**
*Grand Opening Full-Stack Certification | Multi-Agent Concurrent Audit | Financial-Grade Reliability Standard*

---

This is the final and only gate before CoAIleague goes live with its first paying tenant. This audit is unconditional, uncompromising, and non-negotiable in scope. Every layer of the platform — frontend, backend, database, Trinity orchestration, pipelines, automations, financial operations, legal compliance, and security — must be stress tested with real simulated data, verified end-to-end, and confirmed production ready. No phase is skipped. No issue is closed without peer agent verification. No green light is issued with any P0 or P1 open.

CoAIleague handles real organizational money. One miscalculation in payroll or invoicing is not a bug — it is a breach of trust with a paying customer. That standard governs everything that follows.

---

**AGENT DEPLOYMENT MANIFEST**

Deploy all ten agents simultaneously. Every agent shares a single live findings registry. When any agent makes a fix or flags an issue, all agents with downstream exposure immediately re-scan their layer for cascading impact before continuing. No issue is marked resolved until all affected peer agents confirm clean.

---

**AGENT 1 — SEMANTIC AGENT: FINANCIAL INTEGRITY SENTINEL**
*Priority: P0-CRITICAL. This agent never stops running until all others complete.*

Every financial operation must be mathematically accurate, fully persistent, race-condition safe, and legally defensible. A single silent failure here is a production-blocking defect.

- Invoice generation: creation, line item calculation, tax handling, rounding logic, org-scoped persistence, client association, due date logic, and status state machine — draft → sent → paid → overdue — every transition tested
- Payroll calculation engine: gross pay, overtime rules, pay type handling for hourly, salary, and contract workers, deduction logic, pay period alignment with org pay cycle setting, and net pay accuracy verified mathematically
- Payment capture and billing: trial-to-paid conversion, subscription provisioning on payment success, failure handling, retry logic, and payment webhook reliability
- All financial documents generated by Trinity — payroll summaries, invoice PDFs, pay stubs — every figure must be mathematically correct, attributed to the right employee or client, and persist without corruption
- Edge cases: zero-hour shifts, split shifts, mid-period rate changes, retroactive pay adjustments, duplicate invoice prevention, and concurrent write protection on all financial records
- Confirm no financial record can be orphaned, silently overwritten, or deleted without a timestamped audit trail
- Flag any calculation that relies on client-side math rather than server-side validation

Cascading trigger: Any finding immediately notifies Agents 3, 4, and 7.

---

**AGENT 2 — SEMANTIC AGENT: TRINITY INTELLIGENCE VALIDATOR**
*Every Trinity action must produce accurate, trustworthy, org-scoped outputs and persist them correctly*

- Execute every action in the Trinity action registry sequentially with real simulated payloads
- Each action must receive correct input, execute its intended logic, produce its expected output, save that output to the correct record, and confirm completion state
- Test Trinity's natural language instruction handling — issue real org setup commands, scheduling requests, client setup instructions, and staffing directives; verify she executes correctly, not approximately
- Validate Trinity's org learning pipeline: document ingestion, context retention across sessions, and accurate recall of org-specific data during action execution
- Confirm Trinity never generates a financial figure, schedule entry, or document without sourcing from verified org data — no hallucinated values under any condition
- Simulate interrupted sessions — confirm Trinity resumes with full awareness of prior state and outstanding tasks
- Test inbound email processing: calloffs@, incidents@, docs@, support@ — confirm Trinity parses, processes, and acts on each routing correctly and autonomously
- Confirm Trinity's auto-schedule and auto-fill logic respects pay rates, positions, availability, client requirements, and all org rules
- Flag any action that produces a payload but fails to persist it, or persists it to the wrong record

Cascading trigger: Any finding immediately notifies Agents 1, 5, and 8.

---

**AGENT 3 — SYSTEMATIC AGENT: DATABASE & PERSISTENCE INTEGRITY CRAWLER**
*Every byte written must be queryable, accurate, workspace-scoped, and durable across sessions*

- Crawl every database table and confirm workspace_id is present, indexed, and enforced at the query level on every operation — no cross-tenant data leakage possible at any path
- Verify all foreign key relationships are intact — no orphaned records, no nullable foreign keys that should be required
- Confirm every form submission, Trinity action output, pipeline stage transition, and document upload writes to the database and is immediately retrievable
- Test hard refresh survival — every piece of state that should persist must be readable from the database after a full session reset, not reconstructed from memory or local state
- Audit all NOT NULL constraints — no column that should always carry a value can silently accept null
- Scan for race conditions on concurrent writes: two managers filling the same shift simultaneously, two payroll runs triggering at once, duplicate invoice creation under concurrent load
- Verify all timestamps are stored in UTC and displayed with correct org timezone conversion
- Flag any data written only to UI state and never committed to the database

Cascading trigger: Any finding immediately notifies all other agents.

---

**AGENT 4 — SYSTEMATIC AGENT: API & PIPELINE STRESS TESTER**
*Every endpoint, every pipeline stage, every automation must execute reliably under real load*

- Hit every API endpoint with valid payloads, invalid payloads, missing fields, and malformed data — confirm correct responses, proper HTTP error codes, and zero silent failures
- Run every pipeline end-to-end with simulated data: hiring pipeline, onboarding pipeline, scheduling pipeline, invoicing pipeline, payroll pipeline, and all document workflow pipelines
- Verify every pipeline stage transition: correct trigger, correct data carried forward, correct status update, correct notification fired, no stalled or orphaned records
- Test all automations — correct trigger conditions, full logic execution, no duplicate fires on retry
- Confirm all webhooks handle success, failure, and timeout gracefully with proper retry and dead-letter handling
- Stress test concurrent pipeline execution: multiple orgs running payroll simultaneously, multiple hiring pipelines active in the same workspace
- Flag any endpoint that returns 200 but silently fails to commit its operation to the database

Cascading trigger: Any finding immediately notifies Agents 1, 2, and 3.

---

**AGENT 5 — SYSTEMATIC AGENT: RBAC & SUBSCRIPTION ENFORCEMENT AUDITOR**
*No user touches what they don't own. No org accesses what they haven't paid for.*

- Map every feature, route, API endpoint, and Trinity action to its required role and subscription tier
- Attempt access to every restricted resource from every role level: Owner, Manager, Supervisor, Officer, and unauthenticated — guards must block cleanly with correct messaging and zero data exposure
- Attempt access to every subscription-gated feature from a trial org, a lower-tier org, and an expired org — confirm hard server-side blocks, not just UI hiding
- Confirm RBAC is enforced at the API layer — direct API calls from a lower-privileged role must fail at the server regardless of frontend state
- Test org isolation at every data access point — an authenticated user from Org A must never retrieve, modify, or infer any data from Org B
- Confirm subscription tier is set correctly on trial creation, payment success, upgrade, downgrade, and cancellation — and that it is read from the database, not session state
- Flag any feature that is visually hidden in the UI but still reachable via direct API call

Cascading trigger: Any access control gap is immediately escalated to Agents 1 and 3 as P0-critical.

---

**AGENT 6 — SYSTEMATIC AGENT: TENANT ONBOARDING & FULL USER JOURNEY VALIDATOR**
*The complete path from first visit to fully operational org must execute without a single broken step*

Simulate a brand new organization registering from scratch:

- Trial signup and plan selection — completes fully, provisions the workspace, sets subscription tier correctly, and confirms billing terms are presented clearly and conspicuously before payment capture per FTC Negative Option Rule (2023)
- Org settings: pay cycle, payment method capture, staffing email configuration, and all platform-required org-level settings — save, persist, and sync across sessions and devices
- Org owner invites an end user — invite flow completes end-to-end, email delivers, and acceptance lands the user in the correct workspace with correct role
- New user completes personal setup: pay rate, pay type, position, and role — all fields save, persist, and reflect correctly in the org roster and in Trinity's awareness
- Org adds a new client with full configuration: client pay rate, address, billing schedule, and all settings — persists, syncs, and is accessible to Trinity and all relevant platform features
- Org owner uploads organizational documents for Trinity to ingest — processing pipeline executes, Trinity learns and retains org context, and that context survives session resets
- Confirm complete workspace isolation — all data is properly scoped to the new org at every query level

Cascading trigger: Any onboarding gap immediately notifies Agents 3 and 5.

---

**AGENT 7 — CANONICAL AGENT: DATA FLOW TRACER**
*Follows every piece of data from origin to final resting place across every layer of the system*

- Trace the complete lifecycle for: a new employee record, a new client record, a completed shift, a generated invoice, a processed payroll run, and every Trinity-created document
- For each data type confirm: where it is created, every transformation it passes through, every table it touches, every UI view that displays it, and its final persistent state
- Identify every point where any record could be lost, corrupted, duplicated, or incorrectly attributed
- Trace Trinity's data consumption — confirm she reads from live database state, not cached or stale snapshots, when generating schedules, fills, payroll, or documents
- Verify sync between related records — if an employee's pay rate changes mid-period, confirm that change propagates correctly to open shifts, pending payroll, and future invoice calculations with no silent drift
- Trace document generation end-to-end: from Trinity action trigger to payload or PDF creation to storage location to record association to user-visible access
- Flag any data flow that passes through an unvalidated transformation or relies on assumed state rather than queried state
- Re-trace every data flow touched by fixes from any other agent before closing

Cascading trigger: This agent re-traces after every fix made by any other agent.

---

**AGENT 8 — CANONICAL AGENT: SECURITY & INFRASTRUCTURE SCANNER**
*Production systems handling organizational money must meet an uncompromising security baseline*

- Scan all API endpoints for missing authentication middleware — no route reachable without a valid session token unless explicitly designated public
- Verify all user inputs are sanitized server-side before database writes — SQL injection, XSS, and mass assignment vulnerabilities tested on every input surface
- Confirm sensitive data — payment information, pay rates, personal identifiers — is never returned in API responses to roles that don't require it
- Audit all environment variables — confirm no secrets are exposed in client-side bundles, API responses, or error messages
- Verify all financial operations require explicit confirmation before execution — no payroll run or invoice send triggerable by a single accidental click
- Confirm audit logging is active on all financial mutations — every invoice created, edited, or deleted and every payroll run must carry a timestamped record of who executed what
- Test session expiration and token refresh — expired sessions must not continue executing operations
- Flag any endpoint, Trinity action, or automation triggerable externally without authentication

Cascading trigger: Any security gap immediately halts all other agents until resolved.

---

**AGENT 9 — SEMANTIC AGENT: UX & UI STATE CONSISTENCY AUDITOR**
*Every screen must reflect truth. No stale state. No phantom data. No broken flows.*

- Walk every user-facing flow from first click to completion: org signup, user invite acceptance, client setup, document upload, schedule creation, invoice send, and payroll run
- Confirm every form validates correctly — required fields enforced, format rules applied, and clear error messaging on every field type
- Confirm every successful write updates the UI immediately and accurately — no stale data displayed after a mutation
- Test loading states, empty states, and error states on every screen — no blank white pages, no unhandled exceptions surfaced to the user in production
- Confirm all modals, drawers, and dialogs open, close, and reset correctly — no data bleeding between sequential opens of the same component
- Verify mobile responsiveness on every critical workflow — scheduling, timekeeping, and Trinity chat must be fully functional on small screens
- Confirm all navigation guards work: unauthenticated users redirected to login, incomplete onboarding redirected to setup, expired subscriptions redirected to billing
- Flag any UI element displaying a value sourced from local or session state that has not been validated against the database

Cascading trigger: Any finding that touches financial display immediately escalates to Agent 1.

---

**AGENT 10 — CANONICAL AGENT: FTC COMPLIANCE & LEGAL CLAIM AUDITOR**
*Every word the platform says about itself must be accurate, defensible, and legally compliant*

This agent reviews every piece of platform-facing copy, UI label, onboarding text, Trinity dialogue, generated document, and outbound communication against what the platform actually executes. The gap between claim and function is where legal liability lives.

- **AI capability language** — every statement Trinity makes about herself and every label describing her capabilities must reflect only what demonstrably executes. Remove or qualify any language suggesting autonomy, intelligence, or guarantee that is not technically substantiated. Replace superlatives with accurate functional descriptions — "Trinity suggests coverage options" not "Trinity autonomously fills your shifts"
- **Payroll and wage language** — confirm no UI copy, tooltip, or generated document implies legal compliance guarantees. Standard disclaimer required on all payroll-adjacent surfaces: *"CoAIleague is a workforce management tool. Users are responsible for verifying compliance with applicable wage and labor laws."*
- **Data and privacy representations** — audit every instance where data security, privacy, or confidentiality is implied. Confirm infrastructure actually supports every claim made. Flag any claim that exceeds current technical implementation
- **Trial and billing flow** — confirm billing terms are presented clearly and conspicuously before payment capture, not buried in Terms of Service. Auto-renewal and trial-to-paid conversion must be explicitly disclosed at the point of enrollment per FTC Negative Option Rule (2023). Confirm cancellation is as easy to access as signup
- **Hiring pipeline and employment language** — if any copy touches EEOC, background checks, or hiring compliance, confirm it is framed as tooling support only, not legal guidance
- **Outcome and performance claims** — if any marketing, onboarding, or platform copy references results such as time saved or cost reduced, confirm these are substantiated with real data or clearly framed as illustrative examples, never as guaranteed outcomes

Deliverable for this agent: A clean/flag list of every claim in the platform UI and all generated communications. For each flagged item: original language, risk classification (High / Medium / Low), and recommended replacement language that is accurate, legally defensible, and still compelling.

Cascading trigger: Any high-risk claim finding is immediately escalated to Agent 8 and surfaced in the final verdict.

---

**CONCURRENT EXECUTION RULES**

All ten agents run simultaneously and share a single live findings registry. Every issue logged must include:

- **Issue ID** — sequential
- **Severity** — P0 (financial or security), P1 (data loss risk), P2 (functional gap), P3 (UX or cosmetic), P4 (legal claim)
- **Agent** — which agent found it
- **Location** — file, endpoint, component, table, or copy surface
- **Description** — what is broken, missing, or misrepresented and why it matters
- **Cascading Impact** — which peer agents were notified and what they found downstream
- **Fix Applied** — exact change made, or exact prescription if architectural review is required
- **Peer Verification** — confirmed clean by the originating agent and all notified peer agents after fix

No issue is marked resolved until peer verification is complete. No agent terminates early.

---

**FINAL DELIVERABLE**

When all ten agents have completed their sweep and the findings registry shows zero open issues, produce the following structured certification report:

**1. Financial Operations Certification**
Every invoice, payroll, and payment flow confirmed mathematically accurate, fully persistent, and race-condition safe. All financial documents attributed correctly and auditable.

**2. Trinity Certification**
Every registered action verified, every output persisted to the correct record, every financial figure sourced from validated org data with zero hallucinated values.

**3. Tenant Onboarding Certification**
Full org registration, user invite, client setup, and Trinity onboarding flows confirmed end-to-end with no broken steps, no data loss, and no cross-tenant exposure.

**4. Pipeline and Automation Certification**
Every pipeline and automation confirmed to execute fully, transition correctly, and produce durable records with no orphaned or stalled states.

**5. Data Integrity Certification**
Every record workspace-scoped, persistent across sessions, traceable end-to-end, and consistent between Trinity's awareness and the live database.

**6. Security Certification**
Every endpoint authenticated, every input sanitized, every financial mutation logged, and no secrets exposed at any layer.

**7. Legal and Compliance Certification**
Every platform claim verified against actual functionality. All high-risk language remediated. Billing disclosures FTC-compliant. No statement in the platform that cannot be substantiated.

**8. Production Readiness Verdict**
A clear go or hold. If go: confirmation that all ten agents closed with zero open P0 or P1 issues. If hold: a prioritized remediation list with severity, owner, and estimated resolution scope for every remaining item that must be resolved before the first paying tenant goes live.

Do not issue a green light with any P0 or P1 open. Financial accuracy, data integrity, and legal defensibility are non-negotiable. This platform handles real organizational money and the trust of real businesses. The standard is zero.

---

That's your single unified prompt, Bryan. Paste it straight into Replit Agent and let all ten agents run. Bring the findings registry back here and we triage P0s first.