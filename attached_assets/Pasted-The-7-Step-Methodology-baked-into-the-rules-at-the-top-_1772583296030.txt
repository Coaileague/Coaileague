The 7-Step Methodology baked into the rules at the top — Discover, Trace, Verify, Stress, Validate, Document, Fix. The agent must follow this for every single system. No more "looks good" without evidence.
Sections 1-4: New Billing Economy — Schema vs database parity check for all 9 new tables (learned from the org_ledger disaster where the table existed in Drizzle but not in Postgres). Subscription tier seed data verification against exact spec values. Credit drain order test (subscription → carryover → purchased). Race condition test with 10 concurrent deductions. Idempotency test to prevent double-charging. Monthly reset with carryover cap math. Enterprise scaling credits formula verification. Graceful degradation when credits hit zero. Usage cap enforcement — within-cap is free, over-cap costs credits, unavailable features blocked. Financial processing fee tests for invoicing, payroll, and QB sync with deduplication checks. Explicit search to confirm old credit charges for invoicing/payroll/QB are GONE.
Section 5: Mother-Org / Sub-Org — Creates a 3-state test structure (TX/CA/FL, 190 employees). Verifies sub-orgs consume from mother's credit pool. Financial fees recorded per sub-org but billed to mother. Platform bill aggregates everything into one invoice. Multi-state payroll uses correct tax rates per operating_state.
Section 6: Platform Monthly Bill — Full bill generation test with exact math for a Professional org (subscription + overage + 18 invoices + 2 payroll runs + 22 QB syncs + credit pack + add-on). Idempotency test. Fee-to-bill linkage verification. Auto-pay vs manual payment flow.
Section 7: Client & Auditor Portals — Read access is free, DockChat costs 10 credits from the ORG's pool (not the client's), auditor compliance reports cost 10 credits, triggered_by field tracks portal origin.
Section 8: Trinity Autonomous Simulation — THE MAIN EVENT. Full autonomous chain with real simulated data. Run Trinity, then watch the entire pipeline fire: scheduling → time entries → approval → invoicing → financial processing fees (NOT credits) → ledger entries → payroll gate. Then query every table and report the payload data. Verify zero leaks — no unattributed AI calls, no orphaned records, no $0 invoices, no NULL rates, no double-bookings.
Section 9: All 18 Known Past Issues — Every bug we found across every session, re-verified. The Auto-Fill stub. NULL pay rates. Missing ledger table. Payroll without approval gates. Gates not persisting across restart. Sandbox contamination in production. Unattributed AI tokens. Double-booking race condition. Tax stub. Ledger atomicity. Invoice cascade (GAP-016). Seeder overlaps. OT distribution (Robert Williams at 64 hours). Oversize shifts. Workspace guards. Test mode bypass. Raw SQL vs Drizzle. Enum mismatches.
Sections 10-14: Deep System Verification — All financial routes behind RBAC. No dead routes. Webhook signature verification. No hardcoded pricing. Bot execution model and credit consumption. Support agent permissions. Trinity Tier 1/2/3 separation. UNE notification verification with deduplication. Redundant orchestration check (no duplicate events, no overlapping crons). Silent failure hunt — swallowed errors, missing awaits, unchecked nulls, transaction boundary violations.
Section 15: UI Corrections — Searches for every old pricing reference that needs to be removed ("Trinity Thought Stream," "2× credits," old credit allocations). Verifies pricing page reads from database, not hardcoded. Checks credit dashboard exists.
Paste this into Replit and tell it: "Execute sections in order. Report results per section. Fix before moving on. 