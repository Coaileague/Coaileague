/**
 * authCoreRoutes.ts — MERGED into authRoutes.ts (Wave 2 consolidation)
 * Core auth: login, register, logout, MFA, CSRF, dev bypass
 * This stub preserved for backward-compat imports via domains/auth.ts
 */
export { default } from './authRoutes';
